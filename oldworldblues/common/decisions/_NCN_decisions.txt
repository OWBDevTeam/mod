# NCN Conversion Decisions - By HappyNTH #
ncn_conversion = {
	ncn_slow_conversion = { # Converts provinces one by one
		target_trigger = {
			FROM = {
				OR = {
					tag = LON
					tag = IRE
					tag = MUR
					tag = CRZ
					tag = TAR
					tag = SOR
					tag = HGH
					tag = DEA
				}
			}
		}
		visible = {
			if = {
				limit = { FROM = { tag = LON } }
				has_completed_focus = lds_apostle
			}
			if = {
				limit = { FROM = { tag = IRE } }
				has_completed_focus = lds_onegod
			}
			if = {
				limit = { FROM = { tag = MUR } }
				has_completed_focus = lds_heretics
			}
			if = {
				limit = { FROM = { tag = CRZ } }
				has_completed_focus = lds_crazyhorn
			}
			if = {
				limit = { FROM = { tag = TAR } }
				has_completed_focus = lds_tarwalkers
			}
			if = {
				limit = { FROM = { tag = SOR } }
				has_completed_focus = lds_godfather
			}
			if = {
				limit = { FROM = { tag = HGH } }
				has_completed_focus = lds_factions
			}
			if = {
				limit = { FROM = { tag = DEA } }
				has_completed_focus = lds_deadhorse2
			}
			NOT = { FROM = { is_puppet_of = NCN } }
			FROM = { exists = yes }
		}
		available = {
			NOT = {
				FROM = { is_puppet_of = NCN }
				has_war_with = FROM
			}
			NOT = {
				custom_trigger_tooltip = {
					tooltip = hea_not_converted_all_tt
					FROM = {
						all_owned_state = {
							OR = {
								has_state_flag = is_impassable
								has_state_flag = ncn_converted
							}
						}
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = ncn_eligible_tt
				FROM = {
					any_owned_state = {
						any_neighbor_state = {
							OR = {
								has_state_flag = ncn_converted
								is_owned_and_controlled_by = NCN
							}
							NOT = { has_state_flag = is_impassable }
						}
					}
				}
			}
		}

		days_remove = 60
		cost = 10
		ai_will_do = {
			factor = 100
		}

		remove_effect = {
			if = {
				limit = { has_global_flag = debug_logs }
				log = "[GetDateText]: [Root.GetName] ncn_slow_conversion (decision) remove entered"
			}
			FROM = {
				random_owned_state = {
					limit = {
						any_neighbor_state = {
							OR = {
								has_state_flag = ncn_converted
								is_owned_and_controlled_by = NCN
							}
							NOT = { has_state_flag = is_impassable }
						}
						NOT = { has_state_flag = ncn_converted }
						NOT = { has_state_flag = is_impassable }
					}
					set_state_flag = ncn_converted
					custom_effect_tooltip = ncn_converted_state_tt
				}
				#country_event = { id = }
			}
			if = {
				limit = { has_global_flag = debug_logs }
				log = "[GetDateText]: [Root.GetName] ncn_slow_conversion (decision) remove exited"
			}
		}
	}

	ncn_bind_to_us = { # Puppets a country when all the provinces are conquered
		target_trigger = {
			FROM = {
				OR = {
					tag = LON
					tag = IRE
					tag = MUR
					tag = CRZ
					tag = TAR
					tag = SOR
					tag = HGH
					tag = DEA
				}
			}
		}
		visible = {
			if = {
				limit = { FROM = { tag = LON } }
				has_completed_focus = lds_apostle
			}
			if = {
				limit = { FROM = { tag = IRE } }
				has_completed_focus = lds_onegod
			}
			if = {
				limit = { FROM = { tag = MUR } }
				has_completed_focus = lds_heretics
			}
			if = {
				limit = { FROM = { tag = CRZ } }
				has_completed_focus = lds_crazyhorn
			}
			if = {
				limit = { FROM = { tag = TAR } }
				has_completed_focus = lds_tarwalkers
			}
			if = {
				limit = { FROM = { tag = SOR } }
				has_completed_focus = lds_godfather
			}
			if = {
				limit = { FROM = { tag = HGH } }
				has_completed_focus = lds_factions
			}
			if = {
				limit = { FROM = { tag = DEA } }
				has_completed_focus = lds_deadhorse2
			}
			NOT = { FROM = { is_puppet_of = NCN } }
			FROM = { exists = yes }
		}
		available = {
			NOT = {
				has_war_with = FROM
			}
			custom_trigger_tooltip = {
				tooltip = ncn_converted_all_tt
				FROM = {
					all_owned_state = {
						OR = {
							has_state_flag = ncn_converted
							has_state_flag = is_impassable
						}
					}
				}
			}
		}

		days_remove = 20
		cost = 20
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		remove_effect = {
			if = {
				limit = { has_global_flag = debug_logs }
				log = "[GetDateText]: [Root.GetName] ncn_bind_to_us (decision) remove entered"
			}
			FROM = { country_event = { id = nf_mormon.8 } }
			if = {
				limit = { has_global_flag = debug_logs }
				log = "[GetDateText]: [Root.GetName] ncn_bind_to_us (decision) remove exited"
			}
		}
	}
}