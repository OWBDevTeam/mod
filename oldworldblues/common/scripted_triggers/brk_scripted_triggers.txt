brk_vic_raid_available = {
	has_completed_focus = brk_nf_new_victoria
	VIC = { exists = yes } # The target exists - handy
	NOT = { has_war_with = VIC } # We aren't at war with the target
	VIC = {
		NOT = {
			is_subject_of = BRK # They aren't a subject of us
		}
		NOT = {
			is_in_faction_with = BRK # We aren't in a faction with them
		}
	}
	NOT = {
		is_subject_of = VIC # We aren't their subject
	}
	NOT = { has_non_aggression_pact_with = VIC }
}

is_BRK_revolt_effect_valid_country = {
	AND = {
		NOT = { is_in_faction_with = BRK }
		NOT = { is_puppet_of = BRK }
		NOT = { is_subject_of = BRK }
		OR = {
			original_tag = DRE
			original_tag = KAM
			original_tag = WHI
			original_tag = BEL
			original_tag = VIC
			original_tag = HAI
			original_tag = TOC
			original_tag = PMR
		}

	}
}